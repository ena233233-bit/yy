# Python 常用符号速查 😈

## 1. = 赋值
把右边的值放进左边的变量里。

```python
金币 = 20
名字 = "GPT"
```

## 2. == 判断是否相等
一般和 `if` 一起用。

```python
if 选择 == "1":
    送奶茶()
```

意思：选择是不是等于 `"1"`。

## 3. != 判断不相等

```python
if 金币 != 0:
    print("还有钱")
```

## 4. += 增加并保存

```python
金币 += 10
```

等于：

```python
金币 = 金币 + 10
```

## 5. -= 减少并保存

```python
金币 -= 10
```

## 6. + - * / 四则运算

```python
5 + 2
5 - 2
5 * 2
5 / 2
```

分别是：加、减、乘、除。

## 7. > < >= <= 大小比较

```python
金币 > 10
金币 < 10
金币 >= 10
金币 <= 10
```

- `>` 大于
- `<` 小于
- `>=` 大于或等于
- `<=` 小于或等于

## 8. () 小括号
常见用途：调用函数。

```python
print("你好")
input()
送奶茶()
```

`input` 是函数本身，`input()` 才是执行它。

## 9. "" 或 '' 引号
表示文字。

```python
名字 = "GPT"
选择 = '1'
```

没有引号时，Python 通常会把它当变量名。

## 10. : 冒号
`if`、`elif`、`else`、`def`、`while` 后面经常要用。

```python
if 金币 >= 10:
    print("可以买")
```

## 11. , 逗号
用来分开多个东西。

```python
global 纸张好感, 金币
print("金币：", 金币)
```

逗号后面的空格不是必须，但建议打。

## 12. # 注释
`#` 后面的内容不会运行，用来给自己看。

```python
金币 = 20  # 初始金币
```

## 13. 缩进（前面的空格）
Python 里非常重要。进入 `if`、`def` 等代码块后，一般前面打 4 个空格。

```python
if 选择 == "1":
    送奶茶()
```

## 最简单记法

```text
=    给变量一个值
==   问：是不是相等？
!=   问：是不是不相等？
+=   在原来的值上加
-=   在原来的值上减
>    大于
<    小于
>=   大于或等于
<=   小于或等于
()   执行函数 / 放参数
""   文字
:    后面要进入一块代码
#    注释，不运行
```
